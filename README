AFSv1 using libfuse and gRPC

For setup, run setup.sh

Running the server
bazel run //server:server

Running the client
First set 'user_allow_other' in /etc/fuse.conf
Then run: source run.sh

The mountdir is mounted. Since no root dir is specified "/" is treated as the root directory.

